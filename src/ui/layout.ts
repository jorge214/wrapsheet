import { SPACING } from "./metrics";

/**
 * Layout semântico:
 * usa sempre LAYOUT.* nas páginas
 */
export const LAYOUT = {
  pageX: SPACING.pageX ?? 16,        // padding horizontal global
  headerTop: SPACING.headerTop ?? 18,
  sectionGap: SPACING.sectionGap ?? 10,
  cardGap: SPACING.cardGap ?? 10,
};
